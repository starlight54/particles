#ifndef FACTORY_H
#define FACTORY_H
/*
#include <string>
#include <map>

class Factory
{
public:
	template <typename T>
	void Register(const char* iteratorTypeName);
};

template <typename T>
void Factory::Register(const char* typeName)
{
	typeMap[typeName] = &CreateType < T > ;
}
*/
#endif